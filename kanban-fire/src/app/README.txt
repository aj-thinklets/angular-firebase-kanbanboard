Hosting:
- Firebase hosting service

Database:
- For storage we're using cloudfire store highly available 
- It provides client side skd

Firebase CLI.
- Easy deploy and management


========= Dependencies  = ANGULAT + MATERIAL + CDK + FIREBASE ========

1. Angular (below command will scafold the project and install the dependencies)
- ng new <project-name>

2. Angular material (cdk is the dependency of angular material)
- ng add @angular/material

3. Angular Firebase
- ng add @angular/fire



1. Add angular fire in root 
- ng add @angular/fire